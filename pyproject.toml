[tool.ruff]
target-version = "py39"
line-length = 88
# notebooks/ son artefactos de exploración, no código de producción
extend-exclude = ["notebooks/"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]
